<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item android:id="@+id/drawerHomeId"
            android:orderInCategory="1"
            android:title="@string/drawer_menu_home"
            android:icon="@drawable/ic_home"
            app:showAsAction="always|collapseActionView"/>

        <item
            android:id="@+id/drawerFilterId"
            app:showAsAction="always"
            android:orderInCategory="3"
            android:icon="@drawable/ic_filter"
            android:title="@string/drawer_menu_filters"/>

        <item
            android:id="@+id/drawerAboutId"
            app:showAsAction="always"
            android:orderInCategory="4"
            android:icon="@drawable/ic_info_outline"
            android:title="@string/drawer_menu_about">
        </item>

    </group>
</menu>